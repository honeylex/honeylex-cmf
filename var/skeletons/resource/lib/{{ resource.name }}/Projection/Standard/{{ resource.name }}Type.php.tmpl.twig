<?php

namespace {{ crate.namespace }}\{{ resource.name }}\Projection\Standard;

use {{ crate.namespace }}\{{ resource.name }}\Projection\Standard\Base\{{ resource.name }}Type as Base{{ resource.name }}Type;

/**
 * Defines the (normalized) strucuture of a default {{ resource.name|snake }} projection.
 *
 * This class reflects the declared structure of the '{{ resource.name }}'
 * entity. It contains the metadata necessary to initiate and manage the
 * lifecycle of '{{ resource.name }}Entity' instances. Most importantly
 * it holds a collection of attributes (and default attributes) that each of the
 * entities of this type supports.
 *
 * For more information and hooks have a look at the base classes.
 */
class {{ resource.name }}Type extends Base{{ resource.name }}Type
{
    //public function getDefaultAttributes()
    //{
    //    $attributes = parent::getDefaultAttributes();
    //    $attributes['language'] = new Your\Custom\LanguageAttribute('language', array('default' => 'en_UK'));
    //    $attributes['foobar'] = new Your\Custom\FooBarAttribute('foobar');
    //    return $attributes;
    //}
    //
    //protected function getEntityImplementor()
    //{
    //    return '\\Your\\Custom\\Entity';
    //}
}
